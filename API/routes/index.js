// Description: Main app router.
/*
// Imports
var express = require('express'),
	router = express.Router(),
// Import paths to queries
	salaeras = require('./salaeras'),
	tags = require('./tags'),
	users = require('./user'),
	notifications = require('./notifications'),
	session = require('./session');

// Enable queries with use
router.use('/', salaeras);  // 3
router.use('/tags', tags);  // 2
router.use('/users', users);  // 4
router.use('/notifications', notifications);  // 5
router.use('/session', session);  // 1
router.use('/salaeras', salaeras);  // 3, Comments included

module.exports = router;
*/